(function () {
  'use strict';
  const {cards, callout, table, code, flow, notes, slide} = N5.content;
  const s = (id, title, seconds, html, text, extra = {}) => slide(id, title, seconds, html, text, Object.assign({coreSeconds:0},extra));
  N5.chapters.push({id: 7, title: '代码检视多 Agent 设计演示', slides: [
    s('P43', '代码检视：四个视角，一段共同证据', 180,
      code(`@GetMapping("/orders")
public List<OrderVO> list(BigDecimal minAmount,
                          BigDecimal maxAmount, String userId) {
    log.info("query userId={} min={} max={}", userId, minAmount, maxAmount);
    List<Order> orders = orderMapper.selectByAmountRange(minAmount, maxAmount);
    List<OrderVO> result = new ArrayList<>();
    for (Order o : orders) {
        User u = userMapper.selectById(o.getUserId());
        result.add(OrderVO.of(o, u));
    }
    return result;
}`) + callout('同时审查安全、性能、可读性与测试。风险在视角遗漏、口径不同与结论冲突。'),
      notes('这段代码是课程合成 diff，投影省略了参数注解以保证可读性。查询只按金额范围筛选，没有展示登录身份绑定或订单归属条件，userId 还直接来自请求，因此不能信任它作为鉴权依据。循环查用户构成潜在 N+1，缺少分页也是可见风险。amount 是否缺索引需要结合课程提供的建表资料，不能只看 Java 代码就断言。其他切面来自日志规范与测试清单。', '先用 30 秒独立找一个问题，不限维度。然后让四位同学分别代表安全、性能、可读性和测试，只给出一条带证据的发现。', '指认查询行和循环内 selectById 行。说明审查员读取的是同一份冻结 diff 与各自允许的补充资料，避免基于不同版本讨论。', '四份输出并不自动形成有反馈的协作。')),
    s('P44', '四个 prompt 并行，还缺什么协作机制', 120,
      cards([['独立输出后拼接','可以称多 Agent 工作流；但 concat 本身没有解决去重、冲突或验收。'],['有依赖的协作','共享发现、引用他人证据、识别冲突，再让后续判断依赖这些输出。']],2) + callout('如果只需四份独立评分，普通并行代码可能足够；有冲突和复审需求时再设计协作。'),
      notes('不要争论四个 prompt 是否有资格叫多 Agent，这个称呼取决于系统定义。本页关注的是机制增量：简单拼接并没有让审查员互相更新判断，也没有一个统一的合并和验收入口。四个维度在本例确实有独立上下文、工具权限和专业责任；当建议冲突时，还需要共享证据与仲裁。框架是否值得引入取决于这些需求和维护成本。', '安全说必须记录审计，性能说不能同步写入，concat 之后谁来给开发者一个可执行方案？', '对照 P22 三个拆分标准，逐一映射到代码检视角色。', '把并行审查、汇总、仲裁和复审连接成完整闭环。')),
    s('P45', '协作架构：发现汇合之后，还要继续流转', 180,
      flow([['并行审查','四个专业角色'],['汇总与仲裁','去重 / 冲突 / 证据'],['修订','综合方案'],['复审','通过 / 退回']], '代码检视协作闭环', '四个审查员并行产出发现，汇总后由主管基于业务事实仲裁，形成修订方案，再由审查员复审，不通过则回到修订节点。',{target:2,label:'复审不通过 → 退回修订；证据与验收齐备后结束'}) + table(['角色','只读工具 / 资料','不拥有的权限'],[['安全','依赖清单、权限配置、日志规范','直接改生产权限'],['性能','调用链、建表语句、索引信息','删除合规要求'],['可读性','lint 配置、编码规范','忽略安全验收'],['测试','覆盖率报告、用例清单','用“看起来通过”替代测试证据']]),
      notes('Fan-out 是把同一版本输入分发给四个角色，fan-in 是等待约定的报告齐备再汇总。先做去重和严重程度归一化，再判断哪些是事实冲突、哪些只是取舍。Supervisor 根据共享业务事实仲裁，修订交给授权执行方，审查员针对新版本重新给结论。本课用规则事件展示这个协议，并没有让四个模型真实并行读取你的仓库，也不会修改生产代码。', '审查员没有按时回报时，主管能直接默认“无问题”吗？不能，应标记缺失证据，并按超时或人工处理规则结束。', '从图左到右走一遍，再从复审不通过走回修订。指出角色提示中的限制还需要宿主白名单落实。', '先看一个必须基于业务事实处理的真实冲突。')),
    s('P46', '真冲突：审计必须保留，同步写入可以改变', 180,
      cards([['安全诉求','金额区间查询涉及敏感订单数据；课程业务要求必须可审计。'],['性能诉求','高 QPS 列表接口，同步写审计可能增加响应延迟。'],['主管裁决','保留审计；改异步落库，并设计本地兜底队列。']],3) + callout('裁决依据：共享业务事实。验收还要覆盖队列满、重试、重复与恢复后的审计完整性。'),
      notes('这里的审计要求是课程明确给出的业务事实，不是普遍声称所有系统都受同一种合规规则约束。两位审查员的目标都合理，冲突在实现方式。异步加本地兜底只是设计方案，生产上仍要确定持久性、容量、故障恢复、幂等、脱敏和访问控制。如果规则要求严格不丢失，就不能只用内存队列假装可靠。审计记录的受控保存与普通业务日志随意打印敏感数据也应区别。', '如果主通道和本地持久兜底都失败，还能继续查询吗？本案例既定规则是拒绝敏感查询并告警，不得丢弃审计后继续；其他业务须明确自己的规则。', '在共享状态中定位 business_facts.sensitive_order_amounts 和 business_facts.audit_required 两项事实，再查看 decisions 中全部持久接收路径失败时拒绝查询的裁决。', '另一个争议不一定构成真正的性能冲突。')),
    s('P47', '假冲突：用证据检验“多一个对象很慢”', 180,
      cards([['可读性建议','引入 AmountRange，集中校验 min ≤ max 与非负金额。'],['性能顾虑','“新增对象会拖慢接口。”目前只有猜测，没有测量证据。'],['主管处理','保留值对象；优先处理 N+1、分页与索引，再用 profile 验证热点。']],3) + callout('不能仅凭“纳秒对毫秒”断言差六个数量级。分配、GC、JIT 与数据规模都影响结果。'),
      notes('不要把对象创建固定为纳秒、数据库查询固定为毫秒，更不能从单位名称直接推出六个数量级差距。数值和负载未测量时只能提出性能假设。此处驳回的是“仅因多一个对象就否决值对象”的理由，并不宣布对象分配永远没有成本。共享状态同时记录安全审查新增了校验逻辑，性能角色应看到修订后的真实热路径再评价。课堂的规则复审不等于真实压测。', '性能审查员怎样才能让这个反对意见成立？给出可复现负载、profile、分配/GC 指标和对照实验，而不是只说“可能慢”。', '指出共享状态中的 decisions、code_diff、test_plan 与 tests_executed：裁决和示意修订已记录，测试计划仍待执行；tests_executed 为 false，不能把计划当作通过证据。', '演示将两种冲突放在同一条审查与复审链中。')),
    s('P48', '代码检视演示：仲裁后必须复审', 240,
      '<p class="small">并行审查 → 汇总 → 真冲突综合方案 → 无证据顾虑驳回 → 修订 → 规则复审。</p>',
      notes('先运行到四份报告齐备，要求学员指出每条发现的证据来源。停在真冲突，确认主管依据敏感数据和审计要求保留审计，并调整实现方式。再停在 AmountRange 争议，强调结论是当前反对缺少证据，需要 profile。最终修订包含身份绑定、归属过滤、范围校验、分页与查询优化等建议；复审检查课程规则是否满足，不表示代码已经在真实项目编译、压测或通过专业合规审查。', '四位审查员都说“通过”但没有运行任何测试，这算生产可合并吗？不算，报告必须说明验证方式与未完成事项。', '单步到 findings、conflict、arbitration、revision、review 和 done。回看早期事件，确认不会看到后续修订状态。明确页面固定显示预设教学剧本，不将它说成四个真实模型的能力证据。', '回到总图：任务需要什么机制，就增加到哪一层。'),{demo:'review'})
  ]});
})();
